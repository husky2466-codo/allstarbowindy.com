// status.jsx — Status view. Stub. DesignClaude builds the real page here.
function StatusView() {
  return (
    <main className="asb-status">
      <h2>Status</h2>
      <p className="asb-stub-note">Status view stub — DesignClaude builds the real page here.</p>
    </main>
  );
}
window.ASB = window.ASB || {};
window.ASB.StatusView = StatusView;
