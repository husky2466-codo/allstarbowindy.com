// account.jsx — Account view. Stub. DesignClaude builds the real page here.
function AccountView() {
  return (
    <main className="asb-account">
      <h2>Account</h2>
      <p className="asb-stub-note">Account view stub — DesignClaude builds the real page here.</p>
    </main>
  );
}
window.ASB = window.ASB || {};
window.ASB.AccountView = AccountView;
